diff --git a/README.i18n.yaml b/README.i18n.yaml
index 329d0f263..54457d6b3 100644
--- a/README.i18n.yaml
+++ b/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-tool/README.md
-README.md: ac2105c5932717bf58e7bba9268240765c4c3ae3
-README.zh.md: df9a8afe1096856afac6fbc7dfda88a6190d4ba8
+README.md: 9f9ac2b550876884a9e76e5abe1a1e3d5a0fc9a3
+README.zh.md: 9654ab00ac9d2111d423eb1f44c768806c3396fb
diff --git a/README.md b/README.md
index ac2105c59..9f9ac2b55 100644
--- a/README.md
+++ b/README.md
@@ -43,7 +43,7 @@ The owner payload is `ToolCallOwnerProps`: `callId`, `toolName`, the frozen `blo
 
 ### Built-in views
 
-This package owns the generic fallback and the built-in shell/pwsh, read, write/edit, running `str_replace_editor` `create`/`str_replace`, grep/glob, web, todo, question, and Code Dispatch presentations. Structured cards derive directly from first-party raw event fields; Host `presentCall` and `presentResult` values never enter the Client. Foreground one-shot shell results use terminal cards. Settled persistent-shell results use the expandable generic input/output card because reset and partial-output diagnostics do not always describe one process exit status; background acknowledgements remain collapsed. A successful question row pairs call questions with result answers by their stable ids and shows readable question/answer lines when expanded. A cancelled or interrupted row shows its verdict and original questions without inventing answers. Unsupported, malformed, or ambiguous inputs fall back to flattened Tool input/result text. `ui-skill` demonstrates a business-owned registration for `skill`.
+This package owns the generic fallback and the built-in shell/pwsh, read, write/edit, running `str_replace_editor` `create`/`str_replace`, grep/glob, web, todo, question, and Code Dispatch presentations. Structured cards derive directly from first-party raw event fields; Host `presentCall` and `presentResult` values never enter the Client. Foreground one-shot shell results use terminal cards. Settled persistent-shell results use the expandable generic input/output card because reset and partial-output diagnostics do not always describe one process exit status; background acknowledgements remain collapsed. A successful question row pairs call questions with result answers by their stable ids and shows readable question/answer lines when expanded. A cancelled or interrupted row shows its verdict and original questions without inventing answers. Unsupported, malformed, or ambiguous inputs fall back to flattened Tool input/result text. `ui-skill` demonstrates a business-owned registration for `skill`. The generic card is also exported read-only as `GenericToolCard` from `./client` for foreign or sidecar rows: pass the conversation `t`, and omit `openFile`/`inspect` when no Host target exists, so paths stay plain text with no affordance.
 
 -----
 
diff --git a/README.zh.md b/README.zh.md
index df9a8afe1..9654ab00a 100644
--- a/README.zh.md
+++ b/README.zh.md
@@ -43,7 +43,7 @@ owner 载荷为 `ToolCallOwnerProps`：`callId`、`toolName`、冻结的 `block`
 
 ### 内置视图
 
-本包拥有 generic fallback，以及 shell/pwsh、read、write/edit、running `str_replace_editor` `create`／`str_replace`、grep/glob、web、todo、question 与 Code Dispatch 的内置展示。结构化卡片直接从第一方原始 event 字段派生；Host `presentCall` 与 `presentResult` 值不会进入 Client。前台一次性 shell 结果使用 terminal 卡片。已完成的持久 shell 结果使用可展开的 generic 输入／输出卡片，因为 reset 与部分输出诊断不一定描述单个进程的退出状态；后台启动回执保持折叠。成功的问题行按稳定 id 配对调用中的问题与结果中的回答，展开后显示可读的问答行。已取消或已中断的问题行显示其裁决与原始问题，不虚构回答。不受支持、格式错误或含糊的输入回退为压平的工具输入／结果文本。`ui-skill` 展示了业务包自行拥有的 `skill` 注册项。
+本包拥有 generic fallback，以及 shell/pwsh、read、write/edit、running `str_replace_editor` `create`／`str_replace`、grep/glob、web、todo、question 与 Code Dispatch 的内置展示。结构化卡片直接从第一方原始 event 字段派生；Host `presentCall` 与 `presentResult` 值不会进入 Client。前台一次性 shell 结果使用 terminal 卡片。已完成的持久 shell 结果使用可展开的 generic 输入／输出卡片，因为 reset 与部分输出诊断不一定描述单个进程的退出状态；后台启动回执保持折叠。成功的问题行按稳定 id 配对调用中的问题与结果中的回答，展开后显示可读的问答行。已取消或已中断的问题行显示其裁决与原始问题，不虚构回答。不受支持、格式错误或含糊的输入回退为压平的工具输入／结果文本。`ui-skill` 展示了业务包自行拥有的 `skill` 注册项。通用卡片也以只读 `GenericToolCard` 从 `./client` 导出，供外部或 sidecar 行使用：传入 conversation 的 `t`，没有 Host 目标时省略 `openFile`/`inspect`，路径即保持纯文本且无任何操作入口。
 
 -----
 
diff --git a/src/client/index.ts b/src/client/index.ts
index fc65940d0..d094ead10 100644
--- a/src/client/index.ts
+++ b/src/client/index.ts
@@ -3,3 +3,4 @@ export { apply, inject } from './apply.ts'
 export type {
   ToolCallOwnerProps, ToolCallViewProps, ToolDetailsProps, ToolHostInfoInjected, ToolTreeProps,
 } from './contract/slots.ts'
+export { GenericToolCard, type GenericToolCardProps } from './tool/toolviews/GenericToolCard.tsx'
diff --git a/src/client/tool/toolviews/GenericToolCard.tsx b/src/client/tool/toolviews/GenericToolCard.tsx
index 726395d00..885043317 100644
--- a/src/client/tool/toolviews/GenericToolCard.tsx
+++ b/src/client/tool/toolviews/GenericToolCard.tsx
@@ -22,9 +22,17 @@ const VARIANT_ICONS: Record<ToolRowVariant, ReactNode> = {
   others: <IconSparkle16 size={14} />,
 }
 
-/** Card props: the owner payload plus the render site's locale seat (plain prop). */
-export interface GenericToolCardProps extends ToolCallOwnerProps {
+/**
+ * Card props: the owner payload plus the render site's locale seat (plain prop).
+ * The host callbacks are optional at this pure surface: a site without a
+ * filesystem or trajectory target (a sidecar or foreign-runtime row) omits them
+ * and the row degrades to plain text with no affordance. ToolCallTree always
+ * supplies both, so its rows keep the file link and the Inspect pill.
+ */
+export interface GenericToolCardProps extends Omit<ToolCallOwnerProps, 'openFile' | 'inspect'> {
   t: ToolTreeProps['t']
+  openFile?: ToolCallOwnerProps['openFile'] | undefined
+  inspect?: ToolCallOwnerProps['inspect'] | undefined
 }
 
 export function GenericToolCard({ toolName, block, cwd, home, openFile, inspect, t }: GenericToolCardProps) {
diff --git a/tests/coverage-tails.client.spec.tsx b/tests/coverage-tails.client.spec.tsx
index 48703a812..c8b4a5d77 100644
--- a/tests/coverage-tails.client.spec.tsx
+++ b/tests/coverage-tails.client.spec.tsx
@@ -112,4 +112,18 @@ describe('Tool presentation tails', () => {
     expect(stoppedView.container.querySelector('[data-state="stopped"]')).not.toBeNull()
     expect(stoppedView.getByText('已停止')).toBeTruthy()
   })
+
+  it('GenericToolCard without host callbacks renders plain text with no file link or Inspect pill', () => {
+    const settled: ToolResultNode = {
+      kind: 'tool-result', seq: 1, time: 1_000, callId: 'c9',
+      call: { name: 'read', argsRaw: '{"file_path":"/tmp/a.txt"}' },
+      callTime: 500,
+      content: [{ type: 'text', text: 'hello' }], isError: false, subCalls: [],
+    }
+    const view = render(<GenericToolCard callId="c9" toolName="read" block={settled} t={t} />)
+    expect(view.getByText('读取')).toBeTruthy()
+    const path = view.getByText('/tmp/a.txt')
+    expect(path.tagName).toBe('SPAN')
+    expect(view.queryByText('查看')).toBeNull()
+  })
 })
