# ACP-only usage evidence

## Authorized usage-forwarding deployment

The 3082 lab uses `agy_acp_server-ui-usage-v3.par`, combining the approved usage-forwarding patch with trajectory and actual tool-name metadata. The original and UI-only archives remain unchanged. Port 3080 is not changed.

A complete native probe returns this identical result before and after SDK decoding:

```json
{"stopReason":"end_turn","usage":{"totalTokens":10576,"inputTokens":10529,"outputTokens":5,"thoughtTokens":42,"cachedReadTokens":0}}
```

The existing adapter accepts these numeric fields without modification. DSH reports cached input separately and includes reasoning tokens in output once; these values are reported counts, not estimates. A separate real Gemini 3.8 Flash UI turn on 3082 displays `Input 6.5K tok · Output 54 tok` and `Cache hit 63%`. These UI values belong to that separate turn, not the probe above.

The combined runtime passes the native patch/compiler/archive checks and the three live tool/subagent/refresh checks. Private evidence: `/home/noirbright/.dsh/tmp/agy-ui-e2e/acp-usage-enabled-evidence.json`; UI screenshot: `source/.artifacts/native-usage-enabled.png` in the DSH checkout. No Core, usage-monitor, quota, or parser changes are needed for this deployment.

## Baseline: usage patch disabled

One real native turn completed at `2026-09-07T19:02:30.036Z`. The probe used the lab authentication profile and native default configuration, with no tools or delegation requested. The runtime is pinned to `agy_acp_server_20260818_01_RC01`, rebuilt only to forward trajectory and actual tool-name metadata. The usage-forwarding patch is disabled in this baseline probe.

## Every session/update

| Sequence | Tag | Raw update keys | SDK-decoded update keys | Token counters |
| --- | --- | --- | --- | --- |
| 1 | `available_commands_update` | `availableCommands`, `sessionUpdate` | `availableCommands`, `sessionUpdate` | absent |
| 2 | `agent_message_chunk` | `content`, `_meta`, `sessionUpdate` | `_meta`, `content`, `sessionUpdate` | absent |

## Complete session/prompt result

Before SDK decoding:

```json
{"stopReason":"end_turn"}
```

After SDK decoding:

```json
{"stopReason":"end_turn"}
```

Neither result contains a usage field or token counts. No observed token counter was dropped by the SDK in this turn; this does not establish how it handles other versions or populated usage fields. The absence is already present on the ACP wire.

## Baseline finding

The baseline does not justify parser expansion or establish stock ACP token reporting. The authorized deployment above explicitly enables the server-side forwarding patch; it does not change that baseline finding. Neither investigation reads vendor WebSockets or vendor logs, estimates counts, or treats quota as tokens.

The private evidence is `/home/noirbright/.dsh/tmp/agy-ui-e2e/acp-usage-evidence.json` (mode 0600), generated by `scripts/capture-acp-usage.ts`. It contains each update summary and the complete pre/post-SDK prompt result; prompt content, authentication traffic, and stderr are not published. The capture consumes one prompt and refuses to overwrite an existing evidence file.
